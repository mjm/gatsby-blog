import Typography from "typography"

const typography = new Typography({
  baseFontSize: "20px",
  baseLineHeight: 1.5,
  blockMarginBottom: 0.6,
  headerFontFamily: ["Eczar", "serif"],
  headerWeight: 500,
  bodyFontFamily: ["Gentium Basic", "serif"],
})

export default typography
